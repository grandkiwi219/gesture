export const gesture_type: { [key in GestureType]: GestureType } = {
    script: 'script',
    custom_script: 'custom_script'
}

export const bg_consts = {
    event: {
        loaded_command: 'bg_loaded_command'
    },

    size: {
        svg: [640, 640]
    }
}
